# Task preset: Seq2Seq (synthetic), Release profile, auto device

model:
  id: "gpt2"  # any HF causal model for demo
  adapter: "hf_gpt2"
  device: "auto"

dataset:
  provider: "seq2seq"
  seq_len: 128
  stride: 128
  preview_n: 400
  final_n: 400
  seed: 42

eval:
  loss:
    type: "seq2seq"

guards:
  order: ["invariants", "spectral", "rmt", "variance", "invariants"]
