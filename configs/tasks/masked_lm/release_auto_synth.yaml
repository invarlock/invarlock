# Task preset: Masked LM (encoder), Release profile, auto device, synthetic provider

model:
  id: "<set-your-model-id>"   # e.g., bert-base-uncased
  adapter: "hf_bert"
  device: "auto"

dataset:
  provider: "synthetic"
  split: "validation"
  seq_len: 128
  stride: 128
  preview_n: 400
  final_n: 400
  seed: 42

eval:
  loss:
    type: "mlm"
    mask_prob: 0.15
    seed: 42
    random_token_prob: 0.10
    original_token_prob: 0.10

guards:
  order: ["invariants", "spectral", "rmt", "variance", "invariants"]
