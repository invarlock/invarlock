# Edit overlay: RTN dequantized weight-edit simulation, 8-bit, attention-only scope.
#
# Intended for `invarlock evaluate --edit-config ...` (merged with a preset),
# or manual merge into a full run config.

edit:
  name: "quant_rtn"
  plan:
    bitwidth: 8
    per_channel: true
    clamp_ratio: 0.005
    scope: "attn"
    max_modules: 12
    seed: 42
