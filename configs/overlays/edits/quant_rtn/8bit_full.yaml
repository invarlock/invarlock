# Edit overlay: RTN dequantized weight-edit simulation, 8-bit, full-model scope.

edit:
  name: "quant_rtn"
  plan:
    bitwidth: 8
    per_channel: true
    clamp_ratio: 0.005
    scope: "all"
    max_modules: 12
    seed: 42
