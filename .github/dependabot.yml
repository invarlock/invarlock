# Dependabot configuration for automated dependency updates
# https://docs.github.com/en/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file
#
# Repo policy:
# - Version-update PRs target staging/next directly.
# - GitHub security-update PRs still target the default branch (main).
# - Direct Dependabot PRs to main are blocked by the dependabot-main-guard
#   workflow; maintainers must land the equivalent fix on staging/next first
#   and promote it through the normal release flow.

version: 2
updates:
  # Python dependencies (pip)
  - package-ecosystem: "pip"
    directory: "/"
    target-branch: "staging/next"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "America/Toronto"
    open-pull-requests-limit: 5
    reviewers:
      - "invarlock/invarlock-maintainers"
    labels:
      - "dependencies"
      - "python"
    commit-message:
      prefix: "chore(deps)"
    # Group minor/patch updates to reduce PR noise
    groups:
      dev-dependencies:
        patterns:
          - "pytest*"
          - "ruff"
          - "mypy"
          - "mkdocs*"
          - "pre-commit"
        update-types:
          - "minor"
          - "patch"
      ml-dependencies:
        patterns:
          - "torch"
          - "transformers"
          - "numpy"
          - "scipy"
        update-types:
          - "patch"

  # uv lockfile updates for the repo root. Requirements subdirectories are
  # managed by the pip entry above; excluding them prevents Dependabot's uv
  # grapher from treating requirements-only folders as standalone uv projects.
  - package-ecosystem: "uv"
    directory: "/"
    target-branch: "staging/next"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "America/Toronto"
    open-pull-requests-limit: 5
    exclude-paths:
      - "requirements/**"
    reviewers:
      - "invarlock/invarlock-maintainers"
    labels:
      - "dependencies"
      - "python"
      - "uv"
    commit-message:
      prefix: "chore(deps)"

  # GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    target-branch: "staging/next"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "America/Toronto"
    open-pull-requests-limit: 5
    reviewers:
      - "invarlock/invarlock-maintainers"
    labels:
      - "dependencies"
      - "github-actions"
    commit-message:
      prefix: "chore(ci)"
    groups:
      actions:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"

  # npm (for docs tooling: markdownlint, cspell, etc.)
  - package-ecosystem: "npm"
    directory: "/"
    target-branch: "staging/next"
    schedule:
      interval: "monthly"
    open-pull-requests-limit: 3
    reviewers:
      - "invarlock/invarlock-maintainers"
    labels:
      - "dependencies"
      - "npm"
    commit-message:
      prefix: "chore(deps)"
