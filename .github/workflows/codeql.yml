name: CodeQL Security Analysis

on:
  push:
    branches: [ main, staging/next ]
  pull_request:
    branches: [ main, staging/next ]
  # Keep scheduled scans for drift detection and manual trigger for audits.
  schedule:
    # Run weekly on Sunday at 3 AM UTC
    - cron: '0 3 * * 0'
  workflow_dispatch:  # Manual trigger for security audits

permissions:
  contents: read
  actions: read

concurrency:
  group: codeql-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    name: Analyze Python
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
      security-events: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd

      - name: Initialize CodeQL
        uses: github/codeql-action/init@95e58e9a2cdfd71adc6e0353d5c52f41a045d225
        with:
          languages: python
          # Use extended security queries for more thorough analysis
          queries: +security-extended,security-and-quality
          config-file: .github/codeql/codeql-config.yml

      # Autobuild attempts to build any compiled languages (not needed for Python)
      # but helps CodeQL understand the codebase structure
      - name: Autobuild
        uses: github/codeql-action/autobuild@95e58e9a2cdfd71adc6e0353d5c52f41a045d225

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@95e58e9a2cdfd71adc6e0353d5c52f41a045d225
        with:
          category: "/language:python"
          # Upload SARIF results to GitHub Security tab
          upload: true
