# Base runtime dependency closure for the single first-party release artifact.
# Keep this list equal to [project].dependencies in pyproject.toml.
typer>=0.24.1
click>=8.3.3
cryptography>=50.0.0
rich>=13.0
pyyaml>=6.0.3
jsonschema>=4.0
