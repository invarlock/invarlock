{
  "format_version": "plugin-compatibility-v1",
  "core_abi": "0.1",
  "match_policy": "exact_match",
  "supported_plugin_kinds": [
    "adapters",
    "edits",
    "guards"
  ],
  "failure_mode": "ImportError",
  "failure_message": "ABI mismatch: plugin={plugin_abi} != core={core_abi}",
  "version_bump_rules": [
    "Bump core_abi when adapter, edit, or guard interfaces change incompatibly.",
    "Keep exact-match loading until an explicit compatibility matrix is published.",
    "Treat missing INVARLOCK_CORE_ABI as legacy-compatible; mismatched values fail closed."
  ]
}
