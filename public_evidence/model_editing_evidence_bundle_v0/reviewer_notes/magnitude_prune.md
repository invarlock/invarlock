# Magnitude-Prune Lane

This lane uses the existing tiny GPT-2 external magnitude-prune public run as a
pruning evidence example.

It demonstrates strict report verification, runtime provenance, and signed
evidence-pack packaging for an externally produced subject. Edit success, model
safety, pruning quality, and sparse-runtime acceleration require separate
evidence.
