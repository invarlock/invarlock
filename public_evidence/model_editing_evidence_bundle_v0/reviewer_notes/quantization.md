# Quantization Lane

This lane uses the existing tiny GPT-2 `quant_rtn` public run as a quantization
evidence example.

It demonstrates strict report verification and runtime provenance for a
baseline-vs-subject comparison. Edit success, model safety, production
quantization quality, and deployment performance require separate evidence.
