# LoRA-Merge Lane

This lane uses the public LoRA-merge BYOE fixture as an adapter-merge evidence
example.

It demonstrates optional edit provenance and edit-impact metadata for an
external subject reference. Edit success evidence is the verified
artifact/report structure and baseline-relative regression behavior for the
materialized subject. Benchmark quality, model safety evaluation, and
training-method evaluation require separate evidence in benchmark artifacts.
