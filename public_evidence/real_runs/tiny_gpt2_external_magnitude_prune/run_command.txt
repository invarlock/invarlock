SUBJECT_DIR=${SUBJECT_DIR:-tmp/invarlock-byoe-magnitude-prune-subject}
RUN_DIR=${RUN_DIR:-tmp/invarlock-real-byoe-magnitude-prune}

uv run python public_evidence/real_runs/tiny_gpt2_external_magnitude_prune/external_edit_recipe.py \
  --output-dir "$SUBJECT_DIR" \
  --prune-fraction 0.0025

uv run invarlock evaluate \
  --baseline sshleifer/tiny-gpt2 \
  --subject "$SUBJECT_DIR" \
  --baseline-adapter auto \
  --subject-adapter auto \
  --preset configs/presets/causal_lm/wikitext2_512.yaml \
  --edit-label custom \
  --profile release \
  --assurance strict \
  --execution-mode container \
  --allow-network \
  --out "$RUN_DIR/runs" \
  --report-out "$RUN_DIR/report" \
  --quiet \
  --no-progress
